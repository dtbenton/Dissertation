sum(abs(c) > bb_dif)/length(c),
sum(c < bb_dif)/length(c),
sum(abs(c) < bb_dif)/length(c))
}
}
main_perm_func("gbgr","grgb")
main_perm_func = function(a,b){
c = rep(0,5000)
for(i in 1:nrow(c)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="b"])
dif = x-y
c[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="b"])
# 1- and 2-tailed p-values
c(sum(c > bb_dif)/length(c),
sum(abs(c) > bb_dif)/length(c),
sum(c < bb_dif)/length(c),
sum(abs(c) < bb_dif)/length(c))
}
}
#
# GBGR V GRGB
main_perm_func(gbgr,grgb)
c = rep(0,5000)
nrow(c)
nrows(c)
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:nrow(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="b"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="b"])
# 1- and 2-tailed p-values
c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b))
}
}
b = rep(0,5000)
nrow(b)
b = rep(0,4000)
head(b)
length(b)
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:length(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="b"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="b"])
# 1- and 2-tailed p-values
c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b))
}
}
main_perm_func(gbgr,grgb)
main_perm
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:length(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="b"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="b"])
# 1- and 2-tailed p-values
}
c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b))
}
main_perm_func(gbgr,grgb)
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:length(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="x"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="x"])
# 1- and 2-tailed p-values
}
c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b))
}
main_perm_func(gbgr,grgb)
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:length(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="x"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="x"])
# 1- and 2-tailed p-values
c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b))
}
}
main_perm_func(gbgr,grgb)
main_perm_func = function(a,x){
b = rep(0,5000)
for(i in 1:length(b)){
x = sample(D_tall$measure[D_tall$test.trial.level=="a"])
y = sample(D_tall$measure[D_tall$test.trial.level=="x"])
dif = x-y
b[i] = mean(dif)
# compute actual difference
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="a"]-
D_tall$measure[D_tall$test.trial.level=="x"])
# 1- and 2-tailed p-values
return(c(sum(b > bb_dif)/length(b),
sum(abs(b) > bb_dif)/length(b),
sum(b < bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b)))
}
}
#
# GBGR V GRGB
main_perm_func(gbgr,grgb)
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:5000){
a = sample(D_tall$measure[D_tall$test.trial.level=="x"])
b = sample(D_tall$measure[D_tall$test.trial.level=="y"])
}
bb_dif = D_tall$measure[D_tall$test.trial.level=="x"]-D_tall$measure[D_tall$test.trial.level=="y"]
sum(abs(b) > bb_dif)/5000
}
main_perm_func(gbgr,grgb)
main_perm_func(gbgr,gbrg)
mean(D_tall$measure[D_tall$test.trial.level=="gbgr"])
mean(D_tall$measure[D_tall$test.trial.level=="grgb"])
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:5000){
a = sample(D_tall$measure[D_tall$test.trial.level=="x"])
b = sample(D_tall$measure[D_tall$test.trial.level=="y"])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="x"])-mean(D_tall$measure[D_tall$test.trial.level=="y"])
sum(abs(b) > bb_dif)/5000
}
main_perm_func(gbgr,gbrg)
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level=="x"])
b = sample(D_tall$measure[D_tall$test.trial.level=="y"])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="x"])-mean(D_tall$measure[D_tall$test.trial.level=="y"])
sum(abs(b) > bb_dif)/length(b)
mean(D_tall$measure[D_tall$test.trial.level=="x"])
}
main_perm_func(gbgr,gbrg)
fix(D_tall)
# GBGR V GRGB
main_perm_func("gbgr","gbrg")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
sum(abs(b) > bb_dif)/length(b)
mean(D_tall$measure[D_tall$test.trial.level==x])
}
main_perm_func("gbgr","gbrg")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c(sum(abs(b) > bb_dif)/length(b),sum(b > bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b),sum(b < bb_dif)/length(b))
}
main_perm_func("gbgr","gbrg")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c(sum(abs(b) > bb_dif)/length(b),sum(b > bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b),sum(b < bb_dif)/length(b),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]))
}
main_perm_func("gbgr","gbrg")
main_perm_func("gbgr","grgb")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c(sum(abs(b) > bb_dif)/length(b),sum(b > bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b),sum(b < bb_dif)/length(b),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]),bb_diff)
}
main_perm_func("gbgr","grgb")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c(sum(abs(b) > bb_dif)/length(b),sum(b > bb_dif)/length(b),
sum(abs(b) < bb_dif)/length(b),sum(b < bb_dif)/length(b),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]),bb_dif)
}
main_perm_func("gbgr","grgb")
10.865625-7.115625
main_perm_func("gbgr","grgb")
# GBGR V GBRG
main_perm_func("gbgr","gbrg")
# GBGR V GRBG
main_perm_func("gbgr","grbg")
# GRGB V GBRG
main_perm_func("grgb","gbrg")
# GRGB V GRBG
main_perm_func("grgb","grbg")
# GBRG V GRBG
main_perm_func("gbrg","grbg")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c((sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]),
bb_dif)
}
main_perm_func("gbgr","grgb")
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level=="gbgr"])
b = sample(D_tall$measure[D_tall$test.trial.level=="grgb"])
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level=="gbgr"], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level=="grgb"], na.rm=TRUE)
hist(b)
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x])
b = sample(D_tall$measure[D_tall$test.trial.level==y])
b[i] = mean(a)-mean(b)
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c((sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]),
bb_dif)
}
main_perm_func("gbgr","grgb")
main_perm_func = function(x,y){
set.seed(2019)
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level==x], replace=TRUE)
b = sample(D_tall$measure[D_tall$test.trial.level==y], replace=TRUE)
dif = a - b
b[i] = mean(dif)
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==x], na.rm=TRUE)-mean(D_tall$measure[D_tall$test.trial.level==y], na.rm=TRUE)
c((sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==x]),
mean(D_tall$measure[D_tall$test.trial.level==y]),
bb_dif)
}
main_perm_func("gbgr","grgb")
b = rep(0,5000)
for(i in 1:length(b)){
a = sample(D_tall$measure[D_tall$test.trial.level=="gbgr"], replace=TRUE)
b = sample(D_tall$measure[D_tall$test.trial.level=="grgb"], replace=TRUE)
dif = a - b
b[i] = mean(dif)
}
hist(b)
b = rep(0,5000)
for(i in 1:length(b)){
y = sample(D_tall$measure)
a = sample(y[D_tall$test.trial.level=="gbgr"], replace=TRUE)
b = sample(y[D_tall$test.trial.level=="grgb"], replace=TRUE)
dif = a - b
b[i] = mean(dif)
}
hist(b)
main_perm_func = function(x,y){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$condition, levels=c(x,y))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
hist(b)
}
main_perm_func("gbgr","grgb")
main_perm_func(gbgr,grgb)
main_perm_func(1,2)
main_perm_func = function(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$condition, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
hist(b)
}
main_perm_func("gbgr","grgb")
main_perm_func(gbgr,grgb)
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$condition, levels=c("gbgr","grgb"))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c("gbgr","grgb"))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
hist(b)
lm.fit = lm(D_tall$measure~factor(D_tall$condition, levels=c("gbgr","grgb")))
beta_actual = coef(lm.fit)[2]
lm.fit = lm(D_tall$measure~factor(D_tall$test.trial.level, levels=c("gbgr","grgb")))
beta_actual = coef(lm.fit)[2]
summary(lm.fit)
lm.fit = lme(D_tall$measure~factor(D_tall$test.trial.level, levels=c("gbgr","grgb")),random=~1|ID)
beta_actual = coef(lm.fit)[2]
lme(measure~test.trial.level,
random=~1|ID, na.action=na.exclude,
data=D_tall)
lme(measure~test.trial.level,
random=~1|ID, na.action=na.exclude,
data=D_tall)[2]
names(lme(measure~test.trial.level,
random=~1|ID, na.action=na.exclude,
data=D_tall))
lme(measure~test.trial.level,
random=~1|ID, na.action=na.exclude,
data=D_tall)$coefficients
lme(measure~test.trial.level,
random=~1|ID, na.action=na.exclude,
data=D_tall)$coefficients[2]
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
main_perm_func(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
}
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c("gbgr","grgb"))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
hist(b)
main_perm_func(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
}
main_perm_func = function(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
}
main_perm_func = function(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==a], na.rm=TRUE)-
mean(D_tall$measure[D_tall$test.trial.level==z], na.rm=TRUE)
c((sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==a]),
mean(D_tall$measure[D_tall$test.trial.level==z]),
bb_dif)
}
main_perm_func("gbgr","grgb")
cat(paste("(Legs_2)", sep="\t"))
cat(paste("(Legs_2)"))
main_perm_func = function(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==a], na.rm=TRUE)-
mean(D_tall$measure[D_tall$test.trial.level==z], na.rm=TRUE)
c(cat(paste("(Legs_2)")) (sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==a]),
mean(D_tall$measure[D_tall$test.trial.level==z]),
bb_dif)
}
#
# GBGR V GRGB
main_perm_func("gbgr","grgb")
main_perm_func = function(a,z){
b = rep(0,5000)
for(i in 1:length(b)){
x = factor(D_tall$test.trial.level, levels=c(a,z))
y = sample(D_tall$measure, replace=TRUE)
lm_1 = lm(y ~ x, data=D_tall)
b[i] = coef(lm_1)[2]
}
bb_dif = mean(D_tall$measure[D_tall$test.trial.level==a], na.rm=TRUE)-
mean(D_tall$measure[D_tall$test.trial.level==z], na.rm=TRUE)
c((sum(abs(b) > bb_dif)/length(b)),(sum(b > bb_dif)/length(b)),
sum((abs(b) < bb_dif)/length(b)),(sum(b < bb_dif)/length(b)),
mean(D_tall$measure[D_tall$test.trial.level==a]),
mean(D_tall$measure[D_tall$test.trial.level==z]),
bb_dif)
}
main_perm_func("gbgr","grgb")
# GBGR V GBRG
main_perm_func("gbgr","gbrg")
# GBGR V GRBG
main_perm_func("gbgr","grbg")
# GRGB V GBRG
main_perm_func("grgb","gbrg")
# GRGB V GRBG
main_perm_func("grgb","grbg")
# GBRG V GRBG
main_perm_func("gbrg","grbg")
